version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Print Hello World
          command: echo "Hello, World!"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
